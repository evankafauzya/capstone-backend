# ---- Web framework ---------------------------------------------------------
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
gunicorn==21.2.0       # used with the uvicorn worker class for production
python-multipart>=0.0.9
pydantic>=2.6,<3.0
slowapi>=0.1.9,<1.0    # IP- and token-aware rate limiting

# ---- Test dependencies (pinned but not strictly required at runtime) -------
httpx>=0.27,<1.0       # also pulled by fastapi.testclient
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0

# ---- Configuration ---------------------------------------------------------
python-dotenv==1.0.0

# ---- Computer vision / ML --------------------------------------------------
opencv-python-headless==4.10.0.84
numpy>=2.0.0
scipy>=1.14.0
scikit-learn>=1.5.0
mediapipe>=0.10.9
pillow>=11.0.0
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0
ultralytics>=8.3,<9.0

# ---- Reporting / utils -----------------------------------------------------
reportlab>=4.1.0
requests==2.31.0
pytz==2023.3
